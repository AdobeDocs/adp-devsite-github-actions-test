# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: links.spec.js >> links: /firefly-services/docs/workflow-builder-api/getting-started/create-credentials/
- Location: test/links.spec.js:69:3

# Error details

```
Error: Broken links on stage for /firefly-services/docs/workflow-builder-api/getting-started/create-credentials/

expect(received).toEqual(expected) // deep equality

- Expected  - 1
+ Received  + 3

- Array []
+ Array [
+   "/firefly-services/docs/workflow-builder-api (stage: 404, prod: 200)",
+ ]
```

# Page snapshot

```yaml
- generic [active] [ref=e1]:
  - banner [ref=e2]
  - main [ref=e3]:
    - generic [ref=e4]:
      - generic [ref=e7]:
        - button "Edit in GitHub" [ref=e8] [cursor=pointer]:
          - generic [ref=e9]:
            - img
          - generic [ref=e10]: Edit in GitHub
        - button "Log an issue" [ref=e11] [cursor=pointer]:
          - generic [ref=e12]:
            - img
          - generic [ref=e13]: Log an issue
        - button "Copy as Markdown" [ref=e14] [cursor=pointer]:
          - img [ref=e16]
          - generic [ref=e39]: Copy as Markdown
      - heading "[Admins only] Get credentials" [level=1] [ref=e44]
      - generic [ref=e46]:
        - img
        - paragraph [ref=e48]:
          - text: This page is for organization admins who create project credentials for their teams. If you are a developer and your admin has shared a valid client ID and secret with you, start from the
          - link "Workflow Builder API overview" [ref=e49] [cursor=pointer]:
            - /url: /firefly-services/docs/workflow-builder-api/
          - text: .
      - generic [ref=e53]:
        - heading "Access Adobe Developer Consoleaccess-adobe-developer-console" [level=2] [ref=e54]:
          - text: Access Adobe Developer Console
          - link "access-adobe-developer-console" [ref=e56] [cursor=pointer]:
            - /url: "#access-adobe-developer-console"
            - img [ref=e57]
        - separator [ref=e59]
      - generic [ref=e60]:
        - paragraph [ref=e61]:
          - text: Navigate to the
          - strong [ref=e62]: API and services
          - text: section. Search for
          - strong [ref=e63]: Workflow Builder API
          - text: .
        - paragraph [ref=e64]:
          - img "API and services page — Workflow Builder API card" [ref=e66]
        - paragraph [ref=e67]: If you do not see the Workflow Builder API product card, wait a few minutes and try again. Contact your Adobe representative if the product still does not appear.
      - generic [ref=e71]:
        - heading "Create a new projectcreate-a-new-project" [level=2] [ref=e72]:
          - text: Create a new project
          - link "create-a-new-project" [ref=e74] [cursor=pointer]:
            - /url: "#create-a-new-project"
            - img [ref=e75]
        - separator [ref=e77]
      - generic [ref=e78]:
        - paragraph [ref=e79]:
          - text: When you can see the Workflow Builder API product card, choose
          - strong [ref=e80]: Create project
          - text: .
        - list [ref=e81]:
          - listitem [ref=e82]: Register a project name so you can find the project later in Developer Console. You can change this name later.
          - listitem [ref=e83]:
            - text: In the modal, choose
            - strong [ref=e84]: Save configured API
            - text: . On the next screen, you can see your
            - strong [ref=e85]: client ID
            - text: (API key).
          - listitem [ref=e86]:
            - text: In the left navigation, open
            - strong [ref=e87]: OAuth Server-to-Server
            - text: and choose
            - strong [ref=e88]: Retrieve client secret
            - text: to get your client secret.
        - paragraph [ref=e89]:
          - generic [ref=e90]:
            - img "OAuth Server-to-Server credential page"
        - paragraph [ref=e91]:
          - strong [ref=e92]: To call Workflow Builder API, developers need a valid client ID (API key) and an access token.
          - text: Because organization admins are often the only people who can access these projects in the Console, relying on the
          - strong [ref=e93]: Generate access token
          - text: button on the credential overview page is not ideal for every team.
      - generic [ref=e95]:
        - img
        - paragraph [ref=e97]:
          - text: Instead of sharing access tokens, share the
          - strong [ref=e98]: client ID
          - text: and
          - strong [ref=e99]: client secret
          - text: with developers who need access to the API. They can generate new access tokens programmatically; access tokens expire (for example after 24 hours).
      - generic [ref=e100]:
        - paragraph [ref=e101]: "After you configure your project in Developer Console:"
        - list [ref=e102]:
          - listitem [ref=e103]:
            - text: In the
            - strong [ref=e104]: Generate access tokens
            - text: block, select
            - strong [ref=e105]: View cURL command
            - text: .
          - listitem [ref=e106]: "Copy the command and share it with developers in your organization. It should look similar to the following:"
      - code [ref=e112]: "curl -X POST 'https://ims-na1.adobelogin.com/ims/token/v3' \\ -H 'Content-Type: application/x-www-form-urlencoded' \\ -d 'grant_type=client_credentials&client_id={client_id}&client_secret={client_secret}&scope=openid,AdobeID,session,additional_info,read_organizations,firefly_api,ff_apis'"
      - paragraph [ref=e114]:
        - text: Read more about generating access tokens in the
        - link "Authentication" [ref=e115] [cursor=pointer]:
          - /url: /firefly-services/docs/workflow-builder-api/getting-started/
        - text: guide. There you can also find steps for rotating your client secret programmatically.
    - complementary [ref=e119]
```

# Test source

```ts
  57  |     return res.status();
  58  |   } catch {
  59  |     try {
  60  |       const res = await request.get(url, { timeout: 10000 });
  61  |       return res.status();
  62  |     } catch {
  63  |       return 0;
  64  |     }
  65  |   }
  66  | }
  67  | 
  68  | for (const path of paths) {
  69  |   test(`links: ${path}`, async ({ page }, testInfo) => {
  70  |     const request = page.request;
  71  |     await page.setViewportSize({ width: 1600, height: 900 });
  72  | 
  73  |     // Load prod page
  74  |     const prodRes = await page.goto(`${PROD_BASE}${path}`);
  75  |     const prodPageStatus = prodRes?.status() ?? 0;
  76  |     await page.waitForLoadState('load');
  77  |     const prodLinks = await extractLinks(page);
  78  | 
  79  |     // Load stage page
  80  |     const stageRes = await page.goto(`${STAGE_BASE}${path}`);
  81  |     const stagePageStatus = stageRes?.status() ?? 0;
  82  |     await page.waitForLoadState('load');
  83  |     const stageLinks = await extractLinks(page);
  84  | 
  85  |     // Page itself is missing from stage
  86  |     if (stagePageStatus === 404 && prodPageStatus === 200) {
  87  |       await testInfo.attach('link-report', {
  88  |         body: [
  89  |           '❌ PAGE MISSING FROM STAGE',
  90  |           '',
  91  |           `  Page: ${STAGE_BASE}${path}`,
  92  |           `  Prod status:  ${prodPageStatus} OK`,
  93  |           `  Stage status: ${stagePageStatus} Not Found`,
  94  |         ].join('\n'),
  95  |         contentType: 'text/plain',
  96  |       });
  97  |       expect(stagePageStatus, `Page missing from stage: ${STAGE_BASE}${path}`).toBe(200);
  98  |       return;
  99  |     }
  100 | 
  101 |     const prodSet = new Set(prodLinks);
  102 |     const stageSet = new Set(stageLinks);
  103 | 
  104 |     const missingFromStage = prodLinks.filter(l => !stageSet.has(l));
  105 |     const newOnStage = stageLinks.filter(l => !prodSet.has(l));
  106 | 
  107 |     // For each stage link, check if it's a 404 that prod doesn't also have
  108 |     const brokenOnStage = [];
  109 |     await Promise.all(
  110 |       stageLinks.map(async (link) => {
  111 |         const stageUrl = link.startsWith('http') ? link : `${STAGE_BASE}${link}`;
  112 |         const stageStatus = await checkStatus(request, stageUrl);
  113 |         if (stageStatus === 404) {
  114 |           const prodUrl = link.startsWith('http') ? link : `${PROD_BASE}${link}`;
  115 |           const prodStatus = await checkStatus(request, prodUrl);
  116 |           if (prodStatus !== 404) {
  117 |             brokenOnStage.push({ link, stageUrl, stageStatus, prodUrl, prodStatus });
  118 |           }
  119 |         }
  120 |       })
  121 |     );
  122 | 
  123 |     // Build readable report
  124 |     const lines = [`PAGE: ${path}`, ''];
  125 | 
  126 |     if (brokenOnStage.length) {
  127 |       lines.push(`❌ BROKEN LINKS ON STAGE (${brokenOnStage.length}) — ok on prod, 404 on stage:`);
  128 |       for (const { link, stageUrl, stageStatus, prodUrl, prodStatus } of brokenOnStage) {
  129 |         lines.push(`  Link text/href: ${link}`);
  130 |         lines.push(`  Stage URL:      ${stageUrl}  →  ${stageStatus}`);
  131 |         lines.push(`  Prod URL:       ${prodUrl}   →  ${prodStatus}`);
  132 |         lines.push('');
  133 |       }
  134 |     }
  135 | 
  136 |     if (missingFromStage.length) {
  137 |       lines.push(`⚠️  LINKS ON PROD NOT ON STAGE (${missingFromStage.length}) — may indicate missing nav/content:`);
  138 |       for (const l of missingFromStage) lines.push(`  - ${l}`);
  139 |       lines.push('');
  140 |     }
  141 | 
  142 |     if (newOnStage.length) {
  143 |       lines.push(`ℹ️  NEW LINKS ON STAGE NOT ON PROD (${newOnStage.length}):`);
  144 |       for (const l of newOnStage) lines.push(`  + ${l}`);
  145 |     }
  146 | 
  147 |     if (brokenOnStage.length || missingFromStage.length || newOnStage.length) {
  148 |       await testInfo.attach('link-report', {
  149 |         body: lines.join('\n'),
  150 |         contentType: 'text/plain',
  151 |       });
  152 |     }
  153 | 
  154 |     expect(
  155 |       brokenOnStage.map(b => `${b.link} (stage: ${b.stageStatus}, prod: ${b.prodStatus})`),
  156 |       `Broken links on stage for ${path}`
> 157 |     ).toEqual([]);
      |       ^ Error: Broken links on stage for /firefly-services/docs/workflow-builder-api/getting-started/create-credentials/
  158 |   });
  159 | }
  160 | 
```